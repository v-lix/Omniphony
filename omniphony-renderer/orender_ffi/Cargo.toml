[package]
name = "orender_ffi"
version = "0.6.0"
# Edition 2021 (not 2024) so cbindgen sees plain #[no_mangle] and unsafe fn
# bodies stay implicitly unsafe — simpler for a thin C ABI shim.
edition = "2021"
license = "GPL-3.0-or-later"
description = "C ABI for the orender spatial audio renderer (produces liborender.so)"
repository = "https://github.com/mgth/Omniphony"

[lib]
# cdylib produces liborender.so / orender.dll. The rlib lets integration tests
# under tests/ drive the same public entry points from Rust.
name = "orender"
crate-type = ["cdylib", "rlib"]

[dependencies]
orender_engine = { version = "0.2.2", path = "../orender_engine" }
# For build_fingerprint() (orender_build_id): the git-describe stamp lives in
# runtime_control, which both hosts already link — one source of truth.
runtime_control = { version = "0.2.4", path = "../runtime_control" }
anyhow = "1.0"
log = "0.4"
env_logger = "0.11"

[dev-dependencies]
# The OrenderChannelLabel ↔ RChannelLabel discriminant-parity unit test.
bridge_api = { version = "0.4.0", path = "../bridge_api" }
abi_stable = "0.11"
renderer = { version = "0.2.4", path = "../renderer" }

[features]
# SOFA (personalised HRTF) support, on by default: liborender is the desktop
# artifact and the pure-Rust reader adds no system dependency. Embedded
# builds can use --no-default-features.
default = ["sofa"]
sofa = ["orender_engine/sofa"]

[build-dependencies]
cbindgen = "0.27"
